{
  "name": "colrvia-palette-functions",
  "private": true,
  "type": "module",
  "engines": { "node": "^20" },
  "scripts": {
    "build": "tsc -p .",
    "serve": "firebase emulators:start --only functions",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand",
    "lint": "eslint ."
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.12",
    "eslint": "^9.9.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  },
  "jest": {
    "transform": {"^.+\\.(t|j)sx?$": ["ts-jest", {"useESM": true}]},
    "testEnvironment": "node"
  }
}
